<Window x:Class="MediaFileManager.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:MediaFileManager.Desktop.Views"
        mc:Ignorable="d"
        Title="Media File Manager"
        Height="800"
        Width="1375">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <telerik:RadComboBox x:Name="FileTypeComboBox" 
                             SelectedIndex="1"
                             SelectionChanged="FileTypeComboBox_OnSelectionChanged"/>

        <Border x:Name="SelectedViewBorder"
                Grid.Row="1">
            <!--<views:AudioFilesView />
            <views:VideoFilesView />-->
        </Border>
    </Grid>
</Window>
